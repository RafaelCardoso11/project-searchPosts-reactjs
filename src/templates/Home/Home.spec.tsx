import React from 'react';
import { render, screen } from '@testing-library/react';
import {Home} from '.';

describe('<Home />', ()=>{
  it('should do something', () => { //deveria fazer alguma coisa
    expect(1).toBe(1); // eu espero que 1 seja 1
  });
  it('should do something', () => { //deveria fazer alguma coisa
    expect(1).toBe(1); // eu espero que 1 seja 1
  });
})
